# CHANGELOG V33

## Step 4 候選行動方向 API
- 重寫 `/api/generate-actions`，改為真正依據 COCA 表單資料產出 3～6 個候選行動方向。
- API 輸入會整理 Step 0 商圈主副型、最影響生意環境、忙碌時段、購買任務、主要顧客、經營目標、顧客需求、作戰條件、關鍵限制與避開方式。
- Prompt 要求每個候選方案至少連回 3 個本次輸入線索，避免罐頭方案。
- 加入方案品質檢查：過度通用名稱、測試性名稱、輸入線索不足、重複方案會觸發重新生成。
- 預設未設定 `OPENAI_API_KEY` 時不再回傳罐頭方案，而是回傳設定錯誤；如需課堂緊急備援，可設定 `ALLOW_RULE_FALLBACK=true`。
- 新增 `/api/action-health`，供部署後檢查 OpenAI Key 是否已設定。

## 前台修正
- 客群經營優先判斷若最高分同分，不再自動選第一筆；需人工選定本次優先經營客群。
- 方案評估表調整為可在視窗內折行顯示，避免右側欄位被截斷。

## 版面標籤
- 移除頁首右上角「課堂版 V33」標籤，避免線上測試時誤判版本或干擾畫面。
