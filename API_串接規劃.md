# COC 商圈診斷工具｜API 串接規劃

## API 設計原則
1. 前端不得直接呼叫 OpenAI API。
2. OpenAI API Key 必須放在後端環境變數。
3. 每個 AI 按鈕對應一個後端端點。
4. 後端需檢查輸入資料，避免空白或重複呼叫。
5. 需加入防連點、loading 與錯誤訊息。

## 建議 API 端點
- `POST /api/personas`：產出 3 種目標客群情境
- `POST /api/resources`：整理資源與做法
- `POST /api/constraints`：整理條件與限制
- `POST /api/candidate-actions`：產出 4–6 個候選行動方向
- `POST /api/action-design`：整理 Action Design 表格

## 第四階段特別規則
候選行動必須都針對同一組 COC 診斷結果。
不得做成「不同商圈適合不同活動」的比較。
AI 提供候選方向，主管負責採用、修正、合併或放棄。


## 全域 Prompt 規則：必勝客門店空間限制

每次呼叫 AI 時，均應加入以下全域限制：

本工具以必勝客門店情境為主。多數門店以外帶、外送與取餐為主要功能，通常不具備完整內用座位或長時間停留空間。

AI 在產出 COC 診斷與候選行動時，應避免預設門店可以承接長時間內用、久坐、聚會或大型店內活動。

若活動涉及門店前場，必須提醒：
- 需確認前場空間是否足夠
- 活動不得干擾取餐、外送與尖峰營運動線
- 若門店空間不足，應優先改為社區合作、外部場地、市集擺攤、試吃外展、預購或外帶導流

用詞修正：
- 「店內座位或內用空間」改為「門店取餐區與外帶動線」
- 「適合短暫停留與互動」改為「適合短暫接觸、取餐互動或外部活動導流」
- 「戰鬥陀螺來店挑戰」改為「戰鬥陀螺社區合作挑戰」
